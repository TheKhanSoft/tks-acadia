import './bootstrap';

// Import Flatpickr library
import flatpickr from 'flatpickr';

// Optional: Make flatpickr globally available (needed if AlpineJS calls it directly as `flatpickr(...)`)
window.flatpickr = flatpickr;

// You can add other global JS initializations here if needed
